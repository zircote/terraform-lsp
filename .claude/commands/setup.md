---
description: Set up this template (install deps, build, sanity check)
allowed-tools: Bash
---

Run the full local setup for this repo.

!`npm install`
!`npm run typecheck`
!`npm run build`

If anything fails, explain the failure and propose the smallest fix.
