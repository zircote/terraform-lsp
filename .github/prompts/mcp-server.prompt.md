---
title: Add an MCP tool
---

Add a new MCP tool to `src/index.ts`.

Requirements:
- Use `zod` for input schema.
- Return human-readable `content`.
- Add a short section to README describing the tool and example usage.
